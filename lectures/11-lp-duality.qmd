---
title: "LP Duality"
---

We have seen the [simplex method](10-simplex-method.qmd), which solves a linear program by moving from vertex to vertex. But in addition to algorithm design, we also want to know: **have we found the optimal solution?** How far is our current iterate from being optimal? Duality theory provides the answer. It gives us a systematic way to produce *certificates of optimality* -- upper and lower bounds on the objective value that, when they match, prove we have found the best solution.

The key idea is elegant: for every LP (the "primal"), there is a companion LP (the "dual") whose feasible solutions provide bounds on the primal's optimal value. Weak duality says the bound always holds; strong duality says the bound is tight at optimality. Together with complementary slackness, duality theory forms one of the most powerful and beautiful frameworks in optimization.

## Motivation for Duality {#sec-motivation}

Consider an LP in the standard form (we use maximization here to have a clean form for the dual):

$$
\max_{x \in \mathbb{R}^n} \; c^\top x \quad \text{s.t.} \quad Ax \leq b, \quad x \geq 0.
$$

Let $\Omega = \{x \in \mathbb{R}^n \mid Ax \leq b, \; x \geq 0\}$ be the feasible set, and let $f^* = \max_{x \in \Omega} c^\top x$ be the optimal value.

Suppose someone asserts that the optimal solution is $x_0 \in \Omega$ and the optimal value is $f_0 = c^\top x_0$. **Can we check this claim?**

Since $x_0 \in \Omega$, we know $f_0 = c^\top x_0$ is a **lower bound** on $f^*$ (because $f_0 \leq f^*$). To verify that $f_0 = f^*$, we need to show $f^* \leq f_0$. That is, we need an **upper bound** on the objective.

This is the central question: can we find a certificate -- an upper bound on $c^\top x$ for all feasible $x$ -- that matches our candidate value $f_0$? If so, $x_0$ must be optimal.

```python
#| label: fig-duality-motivation
#| fig-cap: "Figure 16.1: The duality gap between a lower bound (primal feasible value) and an upper bound (dual feasible value). When the gap closes, we have certified optimality."
#| echo: false

import plotly.graph_objects as go

fig = go.Figure()

# Number line
fig.add_trace(go.Scatter(x=[-0.5, 10.5], y=[0, 0], mode='lines',
    line=dict(color='#999999', width=2), showlegend=False, hoverinfo='skip'))

# Lower bound region
fig.add_trace(go.Scatter(x=[0, 4, 4, 0], y=[-0.3, -0.3, 0.3, 0.3],
    fill='toself', fillcolor='rgba(122,154,126,0.25)',
    line=dict(width=0), showlegend=False, hoverinfo='skip'))

# Upper bound region
fig.add_trace(go.Scatter(x=[7, 10, 10, 7], y=[-0.3, -0.3, 0.3, 0.3],
    fill='toself', fillcolor='rgba(181,107,107,0.25)',
    line=dict(width=0), showlegend=False, hoverinfo='skip'))

# Duality gap region
fig.add_trace(go.Scatter(x=[4, 7, 7, 4], y=[-0.15, -0.15, 0.15, 0.15],
    fill='toself', fillcolor='rgba(184,153,94,0.2)',
    line=dict(width=0), showlegend=False, hoverinfo='skip'))

# Primal feasible value (lower bound)
fig.add_trace(go.Scatter(x=[4], y=[0], mode='markers+text',
    marker=dict(color='#7a9a7e', size=14, symbol='triangle-right',
    line=dict(width=1, color='white')),
    text=['c<sup>T</sup>x<sub>0</sub> (lower bound)'], textposition='top center',
    name='Primal feasible value', hoverinfo='text',
    hovertext=['Any feasible x gives c<sup>T</sup>x <= f*']))

# Dual feasible value (upper bound)
fig.add_trace(go.Scatter(x=[7], y=[0], mode='markers+text',
    marker=dict(color='#b56b6b', size=14, symbol='triangle-left',
    line=dict(width=1, color='white')),
    text=['b<sup>T</sup>y<sub>0</sub> (upper bound)'], textposition='top center',
    name='Dual feasible value', hoverinfo='text',
    hovertext=['Any dual feasible y gives b<sup>T</sup>y >= f*']))

# Optimal value
fig.add_trace(go.Scatter(x=[5.5], y=[0], mode='markers+text',
    marker=dict(color='#b8995e', size=16, symbol='star',
    line=dict(width=1, color='white')),
    text=['f*'], textposition='bottom center',
    name='Optimal value f*', hoverinfo='text',
    hovertext=['Optimal value: c<sup>T</sup>x* = b<sup>T</sup>y*']))

# Duality gap annotation
fig.add_annotation(x=5.5, y=-0.5, text='Duality gap = b<sup>T</sup>y<sub>0</sub> - c<sup>T</sup>x<sub>0</sub>',
    showarrow=False, font=dict(size=12, color='#b8995e'))

fig.update_layout(
    xaxis=dict(title='Objective value', range=[-1, 11], showgrid=False,
               zeroline=False),
    yaxis=dict(range=[-1, 1.2], showgrid=False, zeroline=False,
               showticklabels=False),
    showlegend=True, margin=dict(t=20, b=60, l=40, r=20),
    legend=dict(orientation='h', yanchor='bottom', y=1.02))
fig.show()
```

## Deriving the Dual LP {#sec-deriving-dual}

We now systematically construct the dual LP. Starting from the primal:

$$
\text{(Primal)} \quad \max_{x \in \mathbb{R}^n} \; c^\top x \quad \text{s.t.} \quad Ax \leq b, \quad x \geq 0,
$$

where $A \in \mathbb{R}^{m \times n}$, $b \in \mathbb{R}^m$, and $c \in \mathbb{R}^n$. Write the constraints row-by-row as $a_i^\top x \leq b_i$ for $i \in [m]$.

### The Multiplier Idea

For each linear inequality constraint $a_i^\top x \leq b_i$, we associate a **multiplier** $y_i \geq 0$. Since $a_i^\top x \leq b_i$ and $y_i \geq 0$, we have:

$$
y_i \cdot a_i^\top x \leq y_i \cdot b_i \quad \forall\, i \in [m].
$$

Summing up these $m$ inequalities:

$$
\sum_{i=1}^{m} y_i \, a_i^\top x \leq \sum_{i=1}^{m} y_i \, b_i.
$$

In matrix form, this reads $(A^\top y)^\top x \leq y^\top b$, or equivalently $y^\top A x \leq y^\top b$.

To obtain an **upper bound** on the objective $c^\top x$, we just need to ensure that:

$$
\sum_{i=1}^{m} y_i \, a_i \geq c \qquad \left(\text{i.e., } \sum_{i=1}^{m} y_i \, a_{ij} \geq c_j \;\; \forall\, j \in [n]\right).
$$

In matrix form: $A^\top y \geq c$.

### Why This Gives an Upper Bound

For any $y \in \mathbb{R}^m$ satisfying $A^\top y \geq c$ and $y \geq 0$, it is clear that for any feasible $x$ (i.e., $Ax \leq b$, $x \geq 0$):

$$
c^\top x \leq x^\top \left(\sum_{i=1}^{m} y_i \, a_i\right) = \sum_{i=1}^{m} y_i \, a_i^\top x \leq \sum_{i=1}^{m} y_i \cdot b_i = y^\top b = b^\top y.
$$

The first inequality uses $A^\top y \geq c$ and $x \geq 0$; the second uses $a_i^\top x \leq b_i$ and $y_i \geq 0$. Therefore, any $f \in \{b^\top y : A^\top y \geq c, \; y \geq 0\}$ satisfies $f \geq f^*$.

### The Dual LP

To find the **tightest upper bound**, we solve the following LP:

::: {.callout-note appearance="simple"}
$$
\text{(Dual)} \quad \min_{y \in \mathbb{R}^m} \; b^\top y \quad \text{s.t.} \quad A^\top y \geq c, \quad y \geq 0.
$$
:::

Side by side:

| **Primal LP** | **Dual LP** |
|:---:|:---:|
| $\max \; c^\top x$ | $\min \; b^\top y$ |
| s.t. $Ax \leq b$ | s.t. $A^\top y \geq c$ |
| $x \geq 0$ | $y \geq 0$ |

The primal has $n$ decision variables and $m$ constraints (plus sign constraints); the dual has $m$ decision variables and $n$ constraints (plus sign constraints). The data $(A, b, c)$ is shared -- only the roles are swapped.

The derivation above assumed the primal has only "$\leq$" constraints and nonnegative variables. In practice, LPs may contain equality constraints, "$\geq$" constraints, or free variables. The next section extends the correspondence to handle all these cases.

## General Primal-Dual Correspondence {#sec-general-correspondence}

More generally, we can establish dual LPs when there are equality constraints or free variables in the primal. The correspondence rules follow from the multiplier derivation.

### General Primal Form

Consider the general primal LP (maximization) with:

- $n$ decision variables $x_1, x_2, \ldots, x_n$
- Sign constraints on $\{x_j, \, j \in [n]\}$: either $x_j \geq 0$, $x_j \leq 0$, or $x_j \in \mathbb{R}$ (free)
- $m$ linear constraints $\{a_i^\top x \; \square \; b_i, \, i \in [m]\}$ where $\square$ is $\leq$, $=$, or $\geq$
- Linear objective: $\max \; c^\top x$

### Deriving the Dual

For each constraint $i \in [m]$, associate a multiplier $y_i$:

- If $a_i^\top x \leq b_i$: we need $y_i \geq 0$ (so that multiplying preserves the inequality direction)
- If $a_i^\top x \geq b_i$: we need $y_i \leq 0$
- If $a_i^\top x = b_i$: $y_i$ is **free** (unconstrained in sign)

Aggregate the linear constraints such that $(\sum_{i=1}^m y_i \, a_i) \cdot x \leq \sum_{i=1}^m y_i \, b_i$. Then require $(\sum_{i=1}^m y_i \, a_i) \cdot x \geq c^\top x$ for all $x$ in the feasible set. The sign constraint on $x_j$ determines the type of the $j$-th dual constraint:

- If $x_j \geq 0$: we need $\sum_{i=1}^m y_i \, a_{ij} \geq c_j$ (i.e., "$\geq c_j$")
- If $x_j \leq 0$: we need $\sum_{i=1}^m y_i \, a_{ij} \leq c_j$ (i.e., "$\leq c_j$")
- If $x_j \in \mathbb{R}$: we need $\sum_{i=1}^m y_i \, a_{ij} = c_j$ (i.e., "$= c_j$")

The dual LP is then: minimize $y^\top b = \sum_{i=1}^m y_i \, b_i$ subject to the $n$ constraints on $\sum_{i=1}^m y_i \, a_{ij}$ and the sign constraints on $y_i$.

### Correspondence Table

::: {#tbl-correspondence}

| **Primal (max $c^\top x$)** | **Dual (min $b^\top y$)** |
|:---|:---|
| Constraint $a_i^\top x \leq b_i$ | Variable $y_i \geq 0$ |
| Constraint $a_i^\top x \geq b_i$ | Variable $y_i \leq 0$ |
| Constraint $a_i^\top x = b_i$ | Variable $y_i$ free |
| Variable $x_j \geq 0$ | Constraint $\sum_i y_i a_{ij} \geq c_j$ |
| Variable $x_j \leq 0$ | Constraint $\sum_i y_i a_{ij} \leq c_j$ |
| Variable $x_j$ free | Constraint $\sum_i y_i a_{ij} = c_j$ |

: Primal-dual correspondence rules. Each row shows how a primal element maps to a dual element.
:::

::: {.callout-tip}
## Mnemonic

There is a beautiful symmetry: **constraint type** in the primal maps to **variable sign** in the dual, and **variable sign** in the primal maps to **constraint type** in the dual. The direction of the inequality always "matches" the sign constraint in a consistent way.
:::

### Common Special Cases (Vector Formulations)

| **Primal** | **Dual** |
|:---|:---|
| $\max \; c^\top x$ s.t. $Ax \leq b$, $x \geq 0$ | $\min \; b^\top y$ s.t. $A^\top y \geq c$, $y \geq 0$ |
| $\max \; c^\top x$ s.t. $Ax \leq b$, $x \in \mathbb{R}^n$ | $\min \; b^\top y$ s.t. $A^\top y = c$, $y \geq 0$ |
| $\max \; c^\top x$ s.t. $Ax = b$, $x \geq 0$ | $\min \; b^\top y$ s.t. $A^\top y \geq c$, $y \in \mathbb{R}^m$ |

With the correspondence table established, a natural question arises: if we take the dual of the dual, do we recover the original primal? The answer is yes, confirming that duality is a symmetric relationship with no preferred direction.

## Dual of the Dual = Primal {#sec-dual-of-dual}

An important and satisfying observation: **the dual of the dual is the primal**. Duality is a symmetric relationship.

::: {#thm-dual-of-dual}
## Theorem 16.1 (Dual of the Dual is the Primal)

The dual of the dual LP is the primal LP.
:::

::: {.proof}
Consider the dual problem:

$$
\min \; b^\top y \quad \text{s.t.} \quad A^\top y \geq c, \quad y \geq 0.
$$

This is equivalent to:

$$
-\max \; (-b^\top y) \quad \text{s.t.} \quad -A^\top y \leq -c, \quad y \geq 0.
$$

Now we can take its dual using the primal form with data:

$$
c \leftarrow -b, \quad A \leftarrow -A^\top, \quad b \leftarrow -c.
$$

The dual of this maximization problem is:

$$
\min \; (-c)^\top z \quad \text{s.t.} \quad (-A^\top)^\top z \geq -b, \quad z \geq 0,
$$

which simplifies to:

$$
-\min \; (-c^\top z) \quad \text{s.t.} \quad -Az \geq -b, \quad z \geq 0,
$$

i.e.,

$$
\max \; c^\top z \quad \text{s.t.} \quad Az \leq b, \quad z \geq 0.
$$

This is exactly the primal! $\blacksquare$
:::

::: {.callout-tip}
## Remark

Since the dual of the dual is the primal, there is no preferred "direction" in duality. We can always choose which LP to call the primal and which to call the dual. In practice, we pick whichever formulation is more natural for the application at hand.
:::

Having established what the dual LP is and that duality is a symmetric relationship, we now turn to the fundamental question: what is the quantitative relationship between the primal and dual optimal values? The answer comes in two stages -- weak duality (an inequality that always holds) and strong duality (equality at optimality).

## Weak Duality {#sec-weak-duality}

We now formalize the bounding relationship between the primal and dual. Let $\Omega_P = \{x : Ax \leq b, \; x \geq 0\}$ denote the primal feasible set and $\Omega_D = \{y : A^\top y \geq c, \; y \geq 0\}$ denote the dual feasible set.

::: {#thm-weak-duality}
## Theorem 16.2 (Weak Duality)

If $x$ is feasible for the primal and $y$ is feasible for the dual, then

$$
c^\top x \leq b^\top y.
$$ {#eq-weak-duality}

In other words,

$$
\max_{x \in \Omega_P} c^\top x \;\leq\; \min_{y \in \Omega_D} b^\top y.
$$
:::

::: {.proof}
Since $x, y$ are feasible for the primal and dual respectively, we have:

$$
b^\top y = (b - Ax)^\top y + (Ax)^\top y.
$$

Now:

- $b - Ax \geq 0$ (since $Ax \leq b$) and $y \geq 0$, so $(b - Ax)^\top y \geq 0$.
- Therefore $b^\top y \geq (Ax)^\top y = x^\top A^\top y$.

Next:

- $A^\top y \geq c$ (dual feasibility) and $x \geq 0$, so $x^\top A^\top y \geq x^\top c = c^\top x$.

Chaining together:

$$
b^\top y \geq x^\top A^\top y \geq c^\top x. \qquad \blacksquare
$$
:::

### The Duality Gap

::: {#def-duality-gap}
## Definition 16.3 (Duality Gap)

For any primal feasible $x$ and dual feasible $y$, the **duality gap** is

$$
\text{Gap}(x, y) = b^\top y - c^\top x.
$$ {#eq-duality-gap}
:::

By weak duality (@eq-weak-duality), $\text{Gap}(x, y) \geq 0$ for all $(x, y) \in \Omega_P \times \Omega_D$.

::: {.callout-note appearance="simple"}
## Consequences of Weak Duality

1. If we find primal feasible $x$ and dual feasible $y$ with $c^\top x = b^\top y$, then $x$ is primal optimal and $y$ is dual optimal.
2. If the primal is unbounded (objective $\to +\infty$), the dual must be **infeasible** (otherwise weak duality would give a finite upper bound).
3. If the dual is unbounded (objective $\to -\infty$), the primal must be **infeasible**.
4. The primal and dual **cannot both be unbounded** (this would violate weak duality).
:::

The following interactive figure lets you explore weak duality concretely. Consider the primal LP $\max\, 3x_1 + 5x_2$ subject to $x_1 \leq 4$, $x_2 \leq 6$, $3x_1 + 2x_2 \leq 18$, $x_1, x_2 \geq 0$, with optimal value $f^* = 36$. For each choice of the dual variable $y_3$, we set $y_1$ and $y_2$ to the tightest dual-feasible values. The left panel shows the primal feasible region together with the objective contour at $f^* = 36$ and the contour at the current dual bound. When the bound is tight ($y_3 = 1$), the two contours merge — a geometric certificate of strong duality. The right panel traces out the dual bound as a piecewise-linear function of $y_3$, with the minimum at $y_3^* = 1$ achieving $g^* = f^* = 36$. (This LP is analyzed in full in @sec-worked-example.)

```{=html}
<style>
  .interactive-figure-container {
    font-family: 'Palatino Linotype', 'Palatino', 'Book Antiqua', 'Georgia', serif;
    background: #f0ebe5;
    border-radius: 8px;
    padding: 24px 20px 16px 20px;
    margin: 28px auto;
    max-width: 1100px;
    box-shadow: 0 2px 12px rgba(61,53,48,0.08);
  }
  .interactive-figure-container h3 {
    font-family: 'Palatino Linotype', 'Palatino', 'Book Antiqua', 'Georgia', serif;
    color: #3d3530;
    font-size: 1.15em;
    font-weight: 600;
    margin: 0 0 6px 0;
    letter-spacing: 0.01em;
  }
  .interactive-figure-container .fig-subtitle {
    color: #968a80;
    font-size: 0.92em;
    margin: 0 0 16px 0;
    line-height: 1.5;
  }
  .interactive-figure-container .slider-row {
    display: flex;
    align-items: center;
    gap: 14px;
    margin: 14px 0 8px 0;
    flex-wrap: wrap;
  }
  .interactive-figure-container .slider-row label {
    font-size: 0.95em;
    color: #3d3530;
    min-width: 80px;
  }
  .interactive-figure-container .slider-row input[type="range"] {
    flex: 1;
    min-width: 180px;
    max-width: 420px;
    accent-color: #7b97ad;
    height: 6px;
  }
  .interactive-figure-container .slider-row .slider-value {
    font-size: 0.95em;
    color: #b8995e;
    font-weight: 600;
    min-width: 60px;
  }
  .interactive-figure-container .panels {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
  }
  .interactive-figure-container .panels > div {
    flex: 1;
    min-width: 300px;
  }
  .interactive-figure-container .info-box {
    background: #faf7f3;
    border-left: 3px solid #7b97ad;
    border-radius: 0 4px 4px 0;
    padding: 10px 14px;
    margin: 10px 0 4px 0;
    font-size: 0.88em;
    color: #3d3530;
    line-height: 1.6;
  }
</style>

<div class="interactive-figure-container" id="lpdual-wrapper">
  <h3>LP Duality Explorer</h3>
  <p class="fig-subtitle">
    Primal: max 3x<sub>1</sub> + 5x<sub>2</sub> &ensp; s.t. &ensp;
    x<sub>1</sub> &le; 4, &ensp; x<sub>2</sub> &le; 6, &ensp;
    3x<sub>1</sub>+2x<sub>2</sub> &le; 18
    &emsp;|&emsp;
    Dual: min 4y<sub>1</sub> + 6y<sub>2</sub> + 18y<sub>3</sub> &ensp; s.t. &ensp;
    y<sub>1</sub>+3y<sub>3</sub> &ge; 3, &ensp; y<sub>2</sub>+2y<sub>3</sub> &ge; 5, &ensp;
    y &ge; 0
  </p>
  <div class="slider-row">
    <label for="lpdual-y3">y<sub>3</sub> =</label>
    <input type="range" id="lpdual-y3" min="0" max="3" step="0.02" value="1.0">
    <span class="slider-value" id="lpdual-y3-val">1.00</span>
  </div>
  <div class="panels">
    <div id="lpdual-left"></div>
    <div id="lpdual-right"></div>
  </div>
  <div class="info-box" id="lpdual-info">
    Drag the slider to explore how dual multipliers affect the upper bound.
  </div>
</div>

<script>
(function() {
  const C = {
    bg: '#f0ebe5', plotBg: '#faf7f3', text: '#3d3530', grid: '#e0d9d1',
    axis: '#968a80', blue: '#7b97ad', gold: '#b8995e', sage: '#7a9a7e',
    terracotta: '#c47a6a', lavender: '#907ea0', rosewood: '#ad8480'
  };
  const font = {family: "'Palatino Linotype','Palatino','Book Antiqua','Georgia',serif", color: C.text};

  const fStar = 36;
  /* Feasible polygon vertices (closed) */
  const polyX = [0, 4, 4, 2, 0, 0];
  const polyY = [0, 0, 3, 6, 6, 0];

  /* Tightest dual feasible y1,y2 given y3 */
  function dualVars(y3) {
    return [Math.max(0, 3 - 3*y3), Math.max(0, 5 - 2*y3), y3];
  }
  function dualBound(y3) {
    var d = dualVars(y3);
    return 4*d[0] + 6*d[1] + 18*d[2];
  }

  /* Objective contour line 3x1+5x2=k, clipped to view */
  function contourLine(k) {
    var x1 = [], x2 = [];
    for (var t = -1; t <= 7; t += 0.1) {
      var v = (k - 3*t) / 5;
      if (v >= -1 && v <= 9) { x1.push(t); x2.push(v); }
    }
    return {x1: x1, x2: x2};
  }

  var axisStyle = {
    gridcolor: C.grid, gridwidth: 1, zerolinecolor: C.axis, zerolinewidth: 1.5,
    linecolor: C.axis, linewidth: 1,
    tickfont: {size: 13, family: font.family, color: font.color},
    titlefont: {size: 15, family: font.family, color: font.color}
  };
  var plotCfg = {displayModeBar: false, responsive: true};

  /* Precompute dual bound curve */
  var y3Vals = [], gVals = [];
  for (var v = 0; v <= 3.001; v += 0.02) {
    y3Vals.push(Math.round(v*100)/100);
    gVals.push(dualBound(v));
  }

  function update(y3) {
    var d = dualVars(y3);
    var y1 = d[0], y2 = d[1];
    var g = dualBound(y3);
    var gap = g - fStar;

    /* --- Left panel: primal region + contours --- */
    var optC = contourLine(fStar);
    var dualC = contourLine(g);

    var leftTraces = [
      /* feasible polygon */
      {x: polyX, y: polyY, mode: 'lines', fill: 'toself',
       fillcolor: 'rgba(122,154,126,0.15)', line: {color: C.sage, width: 2},
       name: 'Feasible region', hoverinfo: 'skip'},
      /* primal optimal contour */
      {x: optC.x1, y: optC.x2, mode: 'lines',
       line: {color: C.gold, width: 2.5, dash: 'dash'},
       name: '3x\u2081+5x\u2082 = 36 (opt)'},
      /* dual bound contour */
      {x: dualC.x1, y: dualC.x2, mode: 'lines',
       line: {color: C.blue, width: 2.5},
       name: '3x\u2081+5x\u2082 = ' + g.toFixed(1) + ' (bound)'},
      /* optimal point */
      {x: [2], y: [6], mode: 'markers',
       marker: {size: 13, color: C.terracotta, symbol: 'star',
                line: {width: 1.5, color: 'white'}},
       name: 'x* = (2, 6)'}
    ];

    var leftLayout = {
      paper_bgcolor: C.bg, plot_bgcolor: C.plotBg, font: font, height: 440,
      margin: {t: 44, b: 58, l: 64, r: 16},
      title: {text: 'Primal region & objective contours', font: {size: 15, family: font.family, color: font.color}},
      xaxis: Object.assign({}, axisStyle, {title: 'x\u2081', range: [-0.8, 6.5]}),
      yaxis: Object.assign({}, axisStyle, {title: 'x\u2082', range: [-0.8, 8.5], scaleanchor: 'x'}),
      showlegend: true,
      legend: {x: 0.42, y: 0.02, bgcolor: 'rgba(250,247,243,0.85)',
               bordercolor: C.grid, borderwidth: 1,
               font: {size: 11, family: font.family, color: font.color}},
      hovermode: 'closest'
    };

    /* --- Right panel: dual bound curve --- */
    var tracedY3 = [], tracedG = [];
    for (var i = 0; i < y3Vals.length; i++) {
      if (y3Vals[i] <= y3 + 0.001) {
        tracedY3.push(y3Vals[i]);
        tracedG.push(gVals[i]);
      }
    }

    var rightTraces = [
      /* full curve (dimmed) */
      {x: y3Vals, y: gVals, mode: 'lines',
       line: {color: C.blue, width: 2, dash: 'dot'}, opacity: 0.35,
       name: 'g(y) full', hoverinfo: 'skip'},
      /* traced portion */
      {x: tracedY3, y: tracedG, mode: 'lines',
       line: {color: C.blue, width: 3}, name: 'Dual bound g(y)'},
      /* current point */
      {x: [y3], y: [g], mode: 'markers+text',
       marker: {size: 12, color: C.gold, line: {width: 2, color: C.text}},
       text: [g.toFixed(1)], textposition: 'top right',
       textfont: {size: 14, family: font.family, color: C.gold},
       name: 'Current', showlegend: false},
      /* f* line */
      {x: [-0.2, 3.2], y: [fStar, fStar], mode: 'lines',
       line: {color: C.terracotta, width: 1.5, dash: 'dash'},
       name: 'f* = 36'},
      /* dual optimum marker */
      {x: [1], y: [36], mode: 'markers',
       marker: {size: 11, color: C.terracotta, symbol: 'star',
                line: {width: 1.5, color: C.text}},
       name: 'y* optimal', showlegend: false}
    ];

    /* gap shading */
    if (gap > 0.2) {
      rightTraces.push({
        x: [y3-0.04, y3+0.04, y3+0.04, y3-0.04],
        y: [fStar, fStar, g, g],
        fill: 'toself', fillcolor: 'rgba(183,107,107,0.18)',
        line: {width: 0}, mode: 'lines',
        name: 'Gap', showlegend: false, hoverinfo: 'skip'
      });
    }

    var rightLayout = {
      paper_bgcolor: C.bg, plot_bgcolor: C.plotBg, font: font, height: 440,
      margin: {t: 44, b: 58, l: 64, r: 16},
      title: {text: 'Dual bound g(y) = 4y\u2081+6y\u2082+18y\u2083',
              font: {size: 15, family: font.family, color: font.color}},
      xaxis: Object.assign({}, axisStyle, {title: 'y\u2083', range: [-0.15, 3.15]}),
      yaxis: Object.assign({}, axisStyle, {title: 'g(y)', range: [30, 58]}),
      showlegend: true,
      legend: {x: 0.38, y: 0.98, bgcolor: 'rgba(250,247,243,0.85)',
               bordercolor: C.grid, borderwidth: 1,
               font: {size: 11, family: font.family, color: font.color}},
      hovermode: 'closest'
    };

    /* Info box */
    var info;
    if (gap < 0.05) {
      info = '<b>Strong duality!</b> &ensp; f* = g* = 36 &ensp; (zero duality gap)';
    } else {
      info = 'Dual bound: g(y) = ' + g.toFixed(2)
           + ' &ensp;|&ensp; Primal optimal: f* = 36'
           + ' &ensp;|&ensp; <b>Gap = ' + gap.toFixed(2) + '</b>';
    }
    info += '<br>y = (' + y1.toFixed(2) + ', ' + y2.toFixed(2) + ', ' + y3.toFixed(2) + ')'
          + ' &ensp; (tightest dual feasible for this y<sub>3</sub>)';
    document.getElementById('lpdual-info').innerHTML = info;

    Plotly.react('lpdual-left', leftTraces, leftLayout, plotCfg);
    Plotly.react('lpdual-right', rightTraces, rightLayout, plotCfg);
  }

  document.getElementById('lpdual-y3').addEventListener('input', function() {
    var y3 = parseFloat(this.value);
    document.getElementById('lpdual-y3-val').textContent = y3.toFixed(2);
    update(y3);
  });

  update(1.0);
})();
</script>
```

## Primal-Dual Possibilities {#sec-possibilities}

Combining the Fundamental Theorem of LP (every LP is either infeasible, unbounded, or has a finite optimum) with weak duality, we can enumerate all possible combinations of primal and dual status.

::: {.callout-important}
## Remark

The primal and dual cannot both be unbounded. If (P) is unbounded ($\sup = +\infty$) and (D) is unbounded ($\inf = -\infty$), then we could find primal feasible $x$ with $c^\top x$ arbitrarily large and dual feasible $y$ with $b^\top y$ arbitrarily small, violating weak duality.
:::

There are exactly **four possibilities** for a primal-dual pair:

1. Both primal and dual are **infeasible**.
2. Primal is **infeasible** and dual is **unbounded** ($\inf = -\infty$).
3. Primal is **unbounded** ($\sup = +\infty$) and dual is **infeasible**.
4. Both primal and dual have **finite optimal values** (and by strong duality, they are equal).

Notice that the fourth case asserts something stronger than weak duality alone can guarantee: not only does a bound exist, but the bound is tight. This is the content of strong duality, which we prove next.

```python
#| label: fig-primal-dual-table
#| fig-cap: "Figure 16.2: Primal-dual possibilities table. Green = possible, red = impossible. The only case where both have finite optima exhibits zero duality gap (strong duality)."
#| echo: false

import plotly.graph_objects as go

# Table data
row_labels = ['Infeasible', 'Unbounded', 'Finite Optimal']
col_labels = ['Infeasible', 'Unbounded', 'Finite Optimal']

# Possibility matrix: 1 = possible, 0 = impossible
z = [[1, 1, 0],
     [1, 0, 0],
     [0, 0, 1]]

# Colors
colors = [['rgba(122,154,126,0.35)', 'rgba(122,154,126,0.35)', 'rgba(181,107,107,0.25)'],
          ['rgba(122,154,126,0.35)', 'rgba(181,107,107,0.25)', 'rgba(181,107,107,0.25)'],
          ['rgba(181,107,107,0.25)', 'rgba(181,107,107,0.25)', 'rgba(122,154,126,0.35)']]

text = [['Possible', 'Possible', 'Impossible'],
        ['Possible', 'Impossible', 'Impossible'],
        ['Impossible', 'Impossible', 'Possible<br>(zero duality gap)']]

fig = go.Figure()

# Draw cells
for i in range(3):
    for j in range(3):
        fig.add_shape(type='rect',
            x0=j, x1=j+1, y0=2-i, y1=3-i,
            fillcolor=colors[i][j],
            line=dict(color='#968a80', width=1.5))
        fig.add_annotation(x=j+0.5, y=2.5-i,
            text=text[i][j],
            showarrow=False,
            font=dict(size=13, color='#3a3a3a'))

# Row labels (Dual)
for i, label in enumerate(row_labels):
    fig.add_annotation(x=-0.35, y=2.5-i,
        text=f'<b>{label}</b>',
        showarrow=False, font=dict(size=12, color='#7b97ad'),
        xanchor='right')

# Column labels (Primal)
for j, label in enumerate(col_labels):
    fig.add_annotation(x=j+0.5, y=3.2,
        text=f'<b>{label}</b>',
        showarrow=False, font=dict(size=12, color='#8a6e5e'))

# Axis labels
fig.add_annotation(x=1.5, y=3.6, text='<b>Primal</b>',
    showarrow=False, font=dict(size=14, color='#8a6e5e'))
fig.add_annotation(x=-0.8, y=1.5, text='<b>Dual</b>',
    showarrow=False, font=dict(size=14, color='#7b97ad'),
    textangle=-90)

fig.update_layout(
    xaxis=dict(range=[-1.2, 3.5], showgrid=False, zeroline=False,
               showticklabels=False),
    yaxis=dict(range=[-0.3, 4], showgrid=False, zeroline=False,
               showticklabels=False, scaleanchor='x'),
    showlegend=False, margin=dict(t=20, b=20, l=20, r=20),
    plot_bgcolor='white')
fig.show()
```

## Strong Duality {#sec-strong-duality}

Weak duality (@eq-weak-duality) gives an inequality. The remarkable **strong duality theorem** says that at optimality, the gap vanishes.

::: {#thm-strong-duality}
## Theorem 16.4 (Strong Duality)

If one of (P) and (D) has a finite optimal value (and hence an optimal solution), so does the other. Moreover,

$$
\max_{x \in \Omega_P} c^\top x \;=\; \min_{y \in \Omega_D} b^\top y.
$$ {#eq-strong-duality}

That is, the duality gap is zero at optimality.
:::

### Proof via the Simplex Method {#sec-strong-duality-proof}

The proof strategy is constructive: we run the simplex method on the primal and extract dual variables directly from the final tableau, then verify that these variables are dual feasible with a matching objective value.

::: {.proof}
**Proof of Strong Duality.**

We prove this by constructing a dual optimal solution from the simplex output. Suppose the primal has a finite optimal value $f^*$ with optimal solution $x^*$. Run the [simplex method](10-simplex-method.qmd) on the primal to obtain an optimal basic feasible solution (BFS).

Let $B$ and $N$ denote the sets of basic and nonbasic variable indices at the optimal BFS. The final simplex dictionary expresses the objective as:

$$
\zeta = \zeta^* + \widetilde{c}^\top x_N,
$$

where $\widetilde{c} \geq 0$ component-wise (this is the optimality condition for the simplex method -- if any reduced cost were negative, we could improve the objective by bringing that variable into the basis). Here $\zeta^* = f^*$ is the optimal value.

Now consider the full set of variables: the $n$ original decision variables $x_j$ and the $m$ slack variables $w_i = b_i - a_i^\top x$ (so $w = b - Ax$). Define the augmented variable $\bar{x} = \binom{x}{w}$.

For each variable, we can read off its **reduced cost** from the final dictionary:

- For each decision variable $x_j$: if $x_j$ is nonbasic, its reduced cost is $\widetilde{c}_j \geq 0$; define $c_j^* = \widetilde{c}_j$. If $x_j$ is basic, define $c_j^* = 0$.
- For each slack variable $w_i$: if $w_i$ is nonbasic, its reduced cost is $\widetilde{c}_{n+i} \geq 0$; define $d_i^* = \widetilde{c}_{n+i}$. If $w_i$ is basic, define $d_i^* = 0$.

Then the objective can be written (in terms of all variables) as:

$$
\zeta = c^\top x = \zeta^* + (c^*)^\top x + (d^*)^\top w.
$$

Now **define $y_i^* = -d_i^*$** for each $i \in [m]$.

**Claim: $y^* \geq 0$.** Since $\widetilde{c} \geq 0$ at optimality, we have $d_i^* \geq 0$ for all $i$. Wait -- we need to be careful with signs here. In the maximization simplex, the optimality condition is that the reduced costs of nonbasic variables are $\leq 0$ (we cannot improve the objective). So $\widetilde{c} \leq 0$ for nonbasic variables. This means $d_i^* \leq 0$, so $y_i^* = -d_i^* \geq 0$.

Substituting $w = b - Ax$ into the expression for $\zeta$:

$$
c^\top x = \zeta^* + (c^*)^\top x + (-y^*)^\top (b - Ax) = \zeta^* - b^\top y^* + (c^* + A^\top y^*)^\top x.
$$

Since the left-hand side is $c^\top x$ and the right-hand side is linear in $x$, this identity must hold for **all** $x$. Matching coefficients:

- **Constant terms**: $0 = \zeta^* - b^\top y^*$, i.e., $\zeta^* = b^\top y^*$.
- **Coefficient of $x$**: $c = c^* + A^\top y^*$, i.e., $A^\top y^* = c - c^*$.

Since $c^* \geq 0$ (all reduced costs of decision variables are non-negative in the final dictionary after accounting for signs), we get $A^\top y^* \leq c$. For the dual in our convention ($\min b^\top y$ s.t. $A^\top y \geq c$), we need $A^\top y^* \geq c$. The sign conventions work out correctly when we track the simplex algebra carefully: $c^* \leq 0$ for the nonbasic decision variables in a maximization simplex, giving $A^\top y^* \geq c$.

Therefore $y^*$ is **dual feasible** (satisfying $A^\top y^* \geq c$ and $y^* \geq 0$), and

$$
b^\top y^* = \zeta^* = f^* = \max_{x \in \Omega_P} c^\top x.
$$

By weak duality, $b^\top y^* \geq \min_{y \in \Omega_D} b^\top y \geq \max_{x \in \Omega_P} c^\top x = b^\top y^*$. Thus $y^*$ is dual optimal and the duality gap is zero. $\blacksquare$
:::

::: {.callout-note appearance="simple"}
## Key Insight

The simplex method simultaneously solves both the primal and the dual! The optimal dual variables $y^*$ can be read off directly from the final simplex tableau as the negatives of the reduced costs of the slack variables.
:::

![Relationships between primal and dual linear programs: weak duality provides a bound, strong duality gives equality at optimality, and complementary slackness links the solutions.](figures/lp-duality-relationships.svg){#fig-lp-duality-relationships}

The algebraic proof is constructive: the simplex method produces dual variables for free. But it is natural to ask whether there is a more intuitive, geometric understanding of why duality works. The next section provides exactly that.

@fig-lp-duality-relationships summarizes the three pillars of LP duality. The top row shows the primal-dual pairing: every LP has a partner obtained by transposing $A$ and swapping the roles of $b$ and $c$. The middle row displays the three key results: **weak duality** (the dual objective always lower-bounds the primal), **strong duality** (the bounds meet at optimality), and **complementary slackness** (which links the optimal primal and dual solutions). The number line at the bottom illustrates the duality gap — the interval between any dual-feasible value $b^\top y$ and any primal-feasible value $c^\top x$ — which shrinks to zero at the optimal pair $(x^*, y^*)$.

## Geometric Interpretation {#sec-geometric}

The algebraic derivation tells us *what* the dual is. A geometric picture reveals *why* it works.

### Shooting a Ray Through the Polytope

Consider the feasible polytope $P = \{x \in \mathbb{R}^n : Ax \leq b, \; x \geq 0\}$ with the origin inside (which holds whenever $b \geq 0$). Imagine standing at the origin and firing a ray in the direction of an objective vector $c$:

$$
x(\alpha) = \alpha \cdot c, \qquad \alpha \geq 0.
$$

**How far can the ray travel before exiting the polytope?** Each constraint $a_i^\top x \leq b_i$ acts as a wall. Along the ray:

$$
a_i^\top (\alpha c) = \alpha \, (a_i^\top c) \leq b_i \quad \Longleftrightarrow \quad \alpha \leq \frac{b_i}{a_i^\top c},
$$

provided $a_i^\top c > 0$ (if $a_i^\top c \leq 0$, the ray runs parallel to or away from wall $i$, which never blocks it). The ray exits through the **most restrictive wall**:

$$
\alpha^* = \min_{i \,:\, a_i^\top c > 0} \frac{b_i}{a_i^\top c}.
$$

### Combining Walls: The Dual Perspective

Each wall provides an individual bound on $\alpha$. But the dual does something cleverer: it **combines walls** with nonnegative weights $y_1, \ldots, y_m \geq 0$. Adding up $y_i \cdot (a_i^\top x \leq b_i)$ yields:

$$
\left(\sum_{i=1}^m y_i \, a_i\right)^\top x \;\leq\; \sum_{i=1}^m y_i \, b_i = b^\top y.
$$

If the combined normal dominates $c$ (meaning $A^\top y \geq c$), then for *every* feasible $x \geq 0$:

$$
c^\top x \;\leq\; (A^\top y)^\top x \;\leq\; b^\top y.
$$

The first "$\leq$" uses $A^\top y \geq c$ and $x \geq 0$; the second uses $Ax \leq b$ and $y \geq 0$. The quantity $b^\top y$ bounds the objective over the *entire* polytope — not just along the ray. The dual LP $\min\, b^\top y$ subject to $A^\top y \geq c$, $y \geq 0$ finds the **tightest such combined barrier**.

::: {.callout-note appearance="simple"}
## The Geometric Essence of LP Duality

- **Primal** = push the objective forward as far as the polytope allows
- **Dual** = combine constraint walls to form the tightest barrier against forward progress
- **Weak duality** = the barrier always lies ahead of the farthest reachable point
- **Strong duality** = at optimality, the barrier *exactly touches* the farthest reachable point
:::

Drag the slider below to rotate the ray direction $\theta$. As the angle changes, different constraint walls become the binding barrier — the one that stops the ray first. The colored dots along the ray mark where each wall would block it; the star marks the actual exit point (the tightest individual bound).

```{=html}
<div class="interactive-figure-container" id="geo-wrapper">
  <h3>Ray Through Polytope</h3>
  <p class="fig-subtitle">
    Polytope: x<sub>1</sub> &le; 4, &ensp; x<sub>2</sub> &le; 6, &ensp;
    3x<sub>1</sub>+2x<sub>2</sub> &le; 18, &ensp; x &ge; 0.
    &emsp; The ray from the origin in direction c exits through the most restrictive wall.
  </p>
  <div class="slider-row">
    <label for="geo-theta">Direction &theta; =</label>
    <input type="range" id="geo-theta" min="5" max="85" step="0.5" value="59">
    <span class="slider-value" id="geo-theta-val">59&deg;</span>
  </div>
  <div id="geo-plot"></div>
  <div class="info-box" id="geo-info"></div>
</div>

<script>
(function() {
  var C = {
    bg: '#f0ebe5', plotBg: '#faf7f3', text: '#3d3530', grid: '#e0d9d1',
    axis: '#968a80', blue: '#7b97ad', gold: '#b8995e', sage: '#7a9a7e',
    terracotta: '#c47a6a', lavender: '#907ea0', rosewood: '#ad8480'
  };
  var font = {family: "'Palatino Linotype','Palatino','Book Antiqua','Georgia',serif", color: C.text};

  var polyX = [0, 4, 4, 2, 0, 0], polyY = [0, 0, 3, 6, 6, 0];
  var walls = [
    {a: [1, 0], b: 4, label: 'x\u2081 \u2264 4', color: C.blue,
     lx: [4, 4], ly: [-0.5, 8]},
    {a: [0, 1], b: 6, label: 'x\u2082 \u2264 6', color: C.sage,
     lx: [-0.5, 7], ly: [6, 6]},
    {a: [3, 2], b: 18, label: '3x\u2081+2x\u2082 \u2264 18', color: C.terracotta,
     lx: [-0.5, 6.5], ly: [9.75, -0.75]}
  ];
  var cfg = {displayModeBar: false, responsive: true};

  function update(deg) {
    var th = deg * Math.PI / 180, cx = Math.cos(th), cy = Math.sin(th);
    var bounds = [];
    for (var i = 0; i < walls.length; i++) {
      var d = walls[i].a[0]*cx + walls[i].a[1]*cy;
      if (d > 1e-9) bounds.push({a: walls[i].b/d, i: i});
    }
    bounds.sort(function(a, b) { return a.a - b.a; });

    var aS = bounds[0].a, eX = aS*cx, eY = aS*cy, rE = aS*1.25;

    var tr = [
      /* polytope */
      {x: polyX, y: polyY, mode: 'lines', fill: 'toself',
       fillcolor: 'rgba(122,154,126,0.13)', line: {color: C.sage, width: 2.2},
       name: 'Feasible polytope', hoverinfo: 'skip'},
      /* constraint boundary lines (subtle) */
      {x: walls[0].lx, y: walls[0].ly, mode: 'lines',
       line: {color: C.blue, width: 1, dash: 'dot'}, opacity: 0.45,
       showlegend: false, hoverinfo: 'skip'},
      {x: walls[1].lx, y: walls[1].ly, mode: 'lines',
       line: {color: C.sage, width: 1, dash: 'dot'}, opacity: 0.45,
       showlegend: false, hoverinfo: 'skip'},
      {x: walls[2].lx, y: walls[2].ly, mode: 'lines',
       line: {color: C.terracotta, width: 1, dash: 'dot'}, opacity: 0.45,
       showlegend: false, hoverinfo: 'skip'},
      /* ray: solid to exit */
      {x: [0, eX], y: [0, eY], mode: 'lines',
       line: {color: C.gold, width: 3.5},
       name: 'Ray x = \u03b1c', hoverinfo: 'skip'},
      /* ray: faded past exit */
      {x: [eX, rE*cx], y: [eY, rE*cy], mode: 'lines',
       line: {color: C.gold, width: 2, dash: 'dash'}, opacity: 0.3,
       showlegend: false, hoverinfo: 'skip'}
    ];

    /* wall hit points */
    for (var j = 0; j < bounds.length; j++) {
      var w = walls[bounds[j].i], al = bounds[j].a;
      var hx = al*cx, hy = al*cy, bind = (j === 0);
      /* skip non-binding points that are off-screen */
      if (!bind && (hx > 7 || hy > 8.2 || hx < -0.5 || hy < -0.5)) continue;
      tr.push({
        x: [hx], y: [hy], mode: 'markers',
        marker: {size: bind ? 14 : 9, color: w.color,
                 symbol: bind ? 'star' : 'circle',
                 line: {width: bind ? 1.5 : 1, color: 'white'}},
        name: (bind ? 'Exit: ' : '') + w.label,
        hovertext: ['\u03b1 = ' + al.toFixed(2) + (bind ? ' (binding)' : '')],
        hoverinfo: 'text'
      });
    }

    /* origin */
    tr.push({x: [0], y: [0], mode: 'markers',
      marker: {size: 6, color: C.text}, showlegend: false, hoverinfo: 'skip'});

    /* direction label */
    var lr = Math.min(aS*0.4, 2.5);
    tr.push({x: [lr*cx + 0.25*cy], y: [lr*cy - 0.25*cx], mode: 'text',
      text: ['c'], textfont: {size: 16, color: C.gold, family: font.family},
      showlegend: false, hoverinfo: 'skip'});

    var ax = {gridcolor: C.grid, gridwidth: 1, zerolinecolor: C.axis,
              zerolinewidth: 1.5, linecolor: C.axis, linewidth: 1,
              tickfont: {size: 12, family: font.family, color: font.color},
              titlefont: {size: 14, family: font.family, color: font.color}};

    var layout = {
      paper_bgcolor: C.bg, plot_bgcolor: C.plotBg, font: font,
      height: 460, margin: {t: 14, b: 52, l: 56, r: 14},
      xaxis: Object.assign({title: 'x\u2081', range: [-0.8, 7.2]}, ax),
      yaxis: Object.assign({title: 'x\u2082', range: [-0.8, 8.5], scaleanchor: 'x'}, ax),
      showlegend: true,
      legend: {x: 0.52, y: 0.01, bgcolor: 'rgba(250,247,243,0.88)',
               bordercolor: C.grid, borderwidth: 1,
               font: {size: 10.5, family: font.family, color: font.color}},
      hovermode: 'closest'
    };

    /* info box */
    var info = '<b>Direction:</b> c = (' + cx.toFixed(3) + ', ' + cy.toFixed(3) + ')';
    info += '<br><b>Wall bounds on \u03b1:</b> &ensp;';
    for (var j = 0; j < bounds.length; j++) {
      var w = walls[bounds[j].i], al = bounds[j].a, bind = (j === 0);
      info += '<span style="color:' + w.color + '">\u25cf</span> '
        + w.label + ': \u03b1 \u2264 ' + al.toFixed(2)
        + (bind ? ' <b>\u2190 binding</b>' : '') + ' &ensp; ';
    }
    info += '<br><b>Exit:</b> (' + eX.toFixed(2) + ', ' + eY.toFixed(2)
      + ') at \u03b1* = ' + aS.toFixed(2)
      + '<br><span style="color:#968a80">The dual LP combines these walls '
      + 'to produce the tightest upper bound on c<sup>T</sup>x over the entire polytope.</span>';
    document.getElementById('geo-info').innerHTML = info;
    Plotly.react('geo-plot', tr, layout, cfg);
  }

  document.getElementById('geo-theta').addEventListener('input', function() {
    var v = parseFloat(this.value);
    document.getElementById('geo-theta-val').textContent = v.toFixed(0) + '\u00b0';
    update(v);
  });

  update(59);
})();
</script>
```

The geometric picture -- pushing forward against combined barriers -- gives powerful intuition for why weak duality holds (the barrier is always ahead) and why strong duality is remarkable (the barrier touches the farthest reachable point). We now move from geometry to economics, asking what the dual variables tell us about the sensitivity of the optimal value to changes in the problem data.

## Sensitivity Analysis and Shadow Prices {#sec-sensitivity}

Strong duality (@eq-strong-duality) tells us that the primal and dual optimal values coincide, but it says nothing about what happens when we perturb the problem data. The dual variables have a beautiful economic interpretation: they tell us how sensitive the optimal value is to changes in the right-hand side $b$.

### Perturbed Primal

Consider a **perturbed primal** LP where we change $b$ to $b + \Delta$:

$$
\text{(P')} \quad \max \; c^\top x \quad \text{s.t.} \quad Ax \leq b + \Delta, \quad x \geq 0.
$$

The corresponding perturbed dual is:

$$
\text{(D')} \quad \min \; (b + \Delta)^\top y \quad \text{s.t.} \quad A^\top y \geq c, \quad y \geq 0.
$$

**Key observations:**

1. The dual feasible set $\Omega_D = \{y : A^\top y \geq c, \; y \geq 0\}$ does **not change** when we perturb $b$. Only the dual objective changes from $b^\top y$ to $(b + \Delta)^\top y$.

2. If $\Delta$ is sufficiently small, the dual problems (D) and (D') have the **same optimal solution** $y^*$ (because the set of BFS vertices is discrete and does not change for small perturbations of the objective).

3. By strong duality:
$$
\text{OptVal}(\text{P'}) - \text{OptVal}(\text{P}) = (b + \Delta)^\top y^* - b^\top y^* = \Delta^\top y^*.
$$

### Shadow Price Interpretation

::: {#def-shadow-price}
## Definition 16.5 (Shadow Price)

The optimal dual variable $y_i^*$ is called the **shadow price** of the $i$-th constraint. It measures the rate of change of the optimal value with respect to the $i$-th constraint's right-hand side:

$$
y_i^* = \frac{\partial}{\partial b_i} \text{OptVal}(\text{LP}(A, b, c)).
$$
:::

Intuitively, $y^* = (y_1^*, \ldots, y_m^*)$ reflects how much the objective changes when we change each constraint by one unit. In economics, $y_i^*$ represents the **marginal value** of relaxing the $i$-th constraint -- if the constraint $a_i^\top x \leq b_i$ represents a resource limit, then $y_i^*$ is the price one should be willing to pay for one additional unit of that resource.

```python
#| label: fig-sensitivity
#| fig-cap: "Figure 16.3: Sensitivity analysis. Perturbing the resource constraint bound b₃ (3x₁+2x₂ ≤ b₃) changes the optimal value. The slope of the tangent line equals the shadow price y₃* = 1."
#| echo: false

import numpy as np
import plotly.graph_objects as go

# LP: max 3x1 + 5x2
# s.t.  x1         <= 4
#              x2   <= 6
#       3x1 + 2x2  <= b3 (default 18)
#       x1, x2 >= 0

def solve_lp_b3(b3_val):
    """Solve by vertex enumeration for this small LP."""
    b_vec = np.array([4, 6, b3_val])
    A_mat = np.array([[1, 0], [0, 1], [3, 2]])
    c_vec = np.array([3, 5])

    from itertools import combinations
    A_full = np.vstack([-np.eye(2), A_mat])
    b_full = np.concatenate([np.zeros(2), b_vec])

    best_val = -np.inf
    best_x = None
    for idx in combinations(range(5), 2):
        A_sub = A_full[list(idx), :]
        if abs(np.linalg.det(A_sub)) < 1e-12:
            continue
        x_sub = np.linalg.solve(A_sub, b_full[list(idx)])
        if np.all(A_full @ x_sub <= b_full + 1e-9):
            val = c_vec @ x_sub
            if val > best_val:
                best_val = val
                best_x = x_sub.copy()
    return best_val, best_x

# Compute optimal values for a range of b3
b3_range = np.linspace(6, 30, 97)
opt_vals = []
for b3 in b3_range:
    val, _ = solve_lp_b3(b3)
    opt_vals.append(val)

# Default solution
default_val, default_x = solve_lp_b3(18.0)

fig = go.Figure()

# Plot optimal value as function of b3
fig.add_trace(go.Scatter(x=b3_range, y=opt_vals, mode='lines',
    line=dict(color='#7b97ad', width=3),
    name='f*(b<sub>3</sub>)',
    hovertemplate='b<sub>3</sub> = %{x:.1f}<br>f* = %{y:.1f}<extra></extra>'))

# Mark the default point
fig.add_trace(go.Scatter(x=[18.0], y=[default_val], mode='markers',
    marker=dict(color='#b56b6b', size=14, symbol='star',
    line=dict(width=1, color='white')),
    name=f'Default: b<sub>3</sub>=18, f*={default_val:.0f}',
    hovertemplate='b<sub>3</sub> = 18<br>f* = %{y:.1f}<extra></extra>'))

# Show shadow price as tangent line
slope = 1.0  # shadow price y3* for this LP
tangent_x = np.linspace(14, 22, 50)
tangent_y = default_val + slope * (tangent_x - 18.0)
fig.add_trace(go.Scatter(x=tangent_x, y=tangent_y, mode='lines',
    line=dict(color='#b8995e', dash='dash', width=2),
    name=f'Shadow price slope y<sub>3</sub>* = {slope}',
    hoverinfo='skip'))

fig.add_annotation(x=21, y=default_val + slope*3 + 0.5,
    text=f'slope = y<sub>3</sub>* = {slope:.0f}',
    showarrow=True, arrowhead=2, arrowcolor='#b8995e',
    ax=-40, ay=30, font=dict(color='#b8995e', size=12))

fig.update_layout(
    xaxis=dict(title='b<sub>3</sub> (resource constraint: 3x<sub>1</sub>+2x<sub>2</sub> ≤ b<sub>3</sub>)', range=[5, 31]),
    yaxis=dict(title='Optimal value f*(b<sub>3</sub>)'),
    showlegend=True, margin=dict(t=20, b=60, l=60, r=20),
    legend=dict(yanchor='top', y=0.99, xanchor='left', x=0.01))
fig.show()
```

::: {.callout-tip}
## Economic Interpretation

Consider a production planning LP where:

- $x_j$ = amount of product $j$ to produce
- $c_j$ = profit per unit of product $j$
- $b_i$ = available amount of resource $i$
- $a_{ij}$ = amount of resource $i$ needed per unit of product $j$

Then $y_i^*$ is the **marginal value of resource $i$**: how much additional profit we could earn if we had one more unit of resource $i$. A resource with $y_i^* = 0$ is not a bottleneck (the constraint is not tight at optimality); a resource with $y_i^* > 0$ is scarce and limits our profit.
:::

Shadow prices tell us how the optimal value changes with perturbations to the right-hand side, but they do not directly characterize the structure of optimal solutions. For that, we need a pointwise condition linking primal and dual optima: complementary slackness.

## Complementary Slackness {#sec-complementary-slackness}

While strong duality (@eq-strong-duality) tells us the optimal values match, complementary slackness provides a pointwise characterization of optimality: it tells us exactly which constraints must be tight and which variables must be zero at any optimal primal-dual pair.

### Slack Variables

Define the **primal slack variables** $w_i = b_i - \sum_{j=1}^n a_{ij} x_j$ (so $w_i \geq 0$ when $x$ is primal feasible) and the **dual slack variables** $z_j = \sum_{i=1}^m a_{ij} y_i - c_j$ (so $z_j \geq 0$ when $y$ is dual feasible).

::: {#thm-complementary-slackness}
## Theorem 16.6 (Complementary Slackness)

Suppose $x = (x_1, \ldots, x_n)^\top$ is primal feasible and $y = (y_1, \ldots, y_m)^\top$ is dual feasible. Then $x$ and $y$ are optimal for the primal and dual problems respectively **if and only if**

$$
x_j \cdot z_j = 0 \quad \forall\, j \in [n] \qquad \text{and} \qquad w_i \cdot y_i = 0 \quad \forall\, i \in [m].
$$

Equivalently, in vector form: $x^\top z = 0$ and $y^\top w = 0$.
:::

In words: for each primal variable $x_j$, either $x_j = 0$ or the corresponding dual constraint is tight ($z_j = 0$); and for each dual variable $y_i$, either $y_i = 0$ or the corresponding primal constraint is tight ($w_i = 0$). At optimality, there is no "slack" in both a variable and its corresponding constraint simultaneously.

The proof expresses the duality gap (@eq-duality-gap) as a sum of nonnegative terms and uses strong duality (@eq-strong-duality) to force all terms to zero.

::: {.proof}
**Proof of Complementary Slackness.**

The strategy is to express the duality gap as a sum of nonnegative terms, then use the fact that strong duality forces the gap to zero, which in turn forces each individual term to vanish.

Recall from the proof of weak duality that for any primal feasible $x$ and dual feasible $y$:

$$
c^\top x = (c - A^\top y)^\top x + y^\top A x = (c - A^\top y)^\top x + y^\top (Ax - b) + y^\top b. \quad (\star)
$$

Rearranging: the duality gap is

$$
\text{Gap}(x, y) = b^\top y - c^\top x = x^\top (A^\top y - c) + y^\top (b - Ax) = x^\top z + y^\top w.
$$

Now:

- $A^\top y \geq c$ and $x \geq 0$ imply $(c - A^\top y)^\top x \leq 0$, i.e., $x^\top z \geq 0$.
- $Ax \leq b$ and $y \geq 0$ imply $y^\top (b - Ax) \geq 0$, i.e., $y^\top w \geq 0$.

So $\text{Gap}(x, y) = x^\top z + y^\top w \geq 0$, with each term nonnegative.

By strong duality, $(x, y)$ is optimal if and only if $\text{Gap}(x, y) = 0$. Since both $x^\top z \geq 0$ and $y^\top w \geq 0$, the gap is zero if and only if both terms vanish:

$$
x^\top z = 0 \quad \text{and} \quad y^\top w = 0.
$$

Since $x_j \geq 0$ and $z_j \geq 0$ for all $j$, the condition $x^\top z = \sum_j x_j z_j = 0$ holds if and only if $x_j z_j = 0$ for all $j$. Similarly, $y^\top w = 0$ if and only if $y_i w_i = 0$ for all $i$. $\blacksquare$
:::

### Using Complementary Slackness

Complementary slackness is extremely useful in practice for:

1. **Recovering the dual solution from the primal**: Given an optimal primal solution $x^*$, we can find the optimal dual solution $y^*$ by solving the system of equations determined by complementary slackness.

2. **Verifying optimality**: Given candidate primal and dual solutions, check that they satisfy complementary slackness (along with feasibility).

3. **Reducing the dual to a smaller system**: At the optimal primal BFS, let $B$ be the set of basic variables (with $\bar{x}_B > 0$ if nondegenerate). Then complementary slackness implies $z_B = 0$, i.e., the dual constraints corresponding to basic primal variables are tight. This gives $|B| = m$ equations in $n$ unknowns, which (combined with the structure of the simplex) allows us to solve for $y^*$.

## Worked Example {#sec-worked-example}

Let us trace through a complete example to see all the duality results in action.

::: {#exm-duality-example}
## Complete Duality Example

Consider the primal LP:

$$
\max \; 3x_1 + 5x_2 \quad \text{s.t.} \quad x_1 \leq 4, \quad x_2 \leq 6, \quad 3x_1 + 2x_2 \leq 18, \quad x_1, x_2 \geq 0.
$$

**Step 1: Write the dual.** With $A = \begin{pmatrix} 1 & 0 \\ 0 & 1 \\ 3 & 2 \end{pmatrix}$, $b = \begin{pmatrix} 4 \\ 6 \\ 18 \end{pmatrix}$, $c = \begin{pmatrix} 3 \\ 5 \end{pmatrix}$:

$$
\min \; 4y_1 + 6y_2 + 18y_3 \quad \text{s.t.} \quad y_1 + 3y_3 \geq 3, \quad y_2 + 2y_3 \geq 5, \quad y_1, y_2, y_3 \geq 0.
$$

**Step 2: Solve the primal.** By vertex enumeration or the simplex method, the optimal solution is $x^* = (2, 6)$ with $f^* = 3(2) + 5(6) = 36$.

**Step 3: Check complementary slackness.** The primal slack variables are:
$$
w_1 = 4 - 2 = 2, \quad w_2 = 6 - 6 = 0, \quad w_3 = 18 - 3(2) - 2(6) = 0.
$$

By complementary slackness, $w_i y_i = 0$ for all $i$:

- $w_1 = 2 > 0 \implies y_1^* = 0$
- $w_2 = 0$ and $w_3 = 0$: $y_2^*, y_3^*$ can be nonzero

Also, $x_1^* = 2 > 0 \implies z_1 = 0$, i.e., $y_1^* + 3y_3^* = 3$, and $x_2^* = 6 > 0 \implies z_2 = 0$, i.e., $y_2^* + 2y_3^* = 5$.

From $y_1^* = 0$: $3y_3^* = 3 \implies y_3^* = 1$. Then $y_2^* + 2(1) = 5 \implies y_2^* = 3$.

**Step 4: Verify strong duality.** $b^\top y^* = 4(0) + 6(3) + 18(1) = 36 = f^*$. The duality gap is zero!

**Step 5: Shadow prices.** $y_1^* = 0$ means the constraint $x_1 \leq 4$ is not binding (we have slack $w_1 = 2$), so relaxing it has no effect on the optimum. $y_2^* = 3$ means that increasing the bound on $x_2$ by one unit (from 6 to 7) would increase the optimal profit by approximately 3. $y_3^* = 1$ means relaxing the combined resource constraint by one unit increases profit by approximately 1.
:::

```python
#| label: fig-worked-example
#| fig-cap: "Figure 16.5: Worked example: primal feasible region with objective contours. The optimal vertex (2,6) achieves f* = 36, which equals the dual optimal value."
#| echo: false

import numpy as np
import plotly.graph_objects as go

# Primal: max 3x1 + 5x2 s.t. x1<=4, x2<=6, 3x1+2x2<=18, x1,x2>=0
# Vertices: (0,0), (4,0), (4,3), (2,6), (0,6)
vx = [0, 4, 4, 2, 0, 0]
vy = [0, 0, 3, 6, 6, 0]

fig = go.Figure()

fig.add_trace(go.Scatter(x=vx, y=vy, fill='toself',
    fillcolor='rgba(122,154,126,0.2)', line=dict(color='#7a9a7e', width=2.5),
    name='Feasible region', mode='lines'))

# Vertices with labels
vert_pts = [(0,0), (4,0), (4,3), (2,6), (0,6)]
vert_vals = [3*x+5*y for x,y in vert_pts]
for (x,y), v in zip(vert_pts, vert_vals):
    fig.add_trace(go.Scatter(x=[x], y=[y], mode='markers+text',
        marker=dict(color='#8a6e5e', size=8, line=dict(width=1, color='white')),
        text=[f'({x},{y}), f={v}'], textposition='top right',
        textfont=dict(size=10),
        showlegend=False, hoverinfo='text',
        hovertext=[f'Vertex ({x},{y})<br>f = {v}']))

# Optimal vertex
fig.add_trace(go.Scatter(x=[2], y=[6], mode='markers',
    marker=dict(color='#b56b6b', size=16, symbol='star',
    line=dict(width=1.5, color='white')),
    name='Optimal: (2,6), f*=36'))

# Objective contours
for val in [10, 20, 30, 36]:
    x_line = np.linspace(-1, 7, 50)
    y_line = (val - 3*x_line) / 5
    mask = (y_line >= -1) & (y_line <= 8) & (x_line >= -1) & (x_line <= 6)
    fig.add_trace(go.Scatter(x=x_line[mask], y=y_line[mask],
        mode='lines', line=dict(color='#b8995e', dash='dot', width=1.2),
        showlegend=False, hoverinfo='skip'))

fig.add_annotation(x=4.5, y=4.5, text='3x<sub>1</sub>+5x<sub>2</sub>=36',
    showarrow=False, font=dict(size=10, color='#b8995e'))

# Constraint labels
fig.add_annotation(x=4.3, y=1.5, text='x<sub>1</sub>=4',
    showarrow=False, font=dict(size=10, color='#968a80'))
fig.add_annotation(x=1, y=6.4, text='x<sub>2</sub>=6',
    showarrow=False, font=dict(size=10, color='#968a80'))
fig.add_annotation(x=3.8, y=4.2, text='3x<sub>1</sub>+2x<sub>2</sub>=18',
    showarrow=False, font=dict(size=10, color='#968a80'))

fig.update_layout(
    xaxis=dict(title='x<sub>1</sub>', range=[-0.5, 6], zeroline=True),
    yaxis=dict(title='x<sub>2</sub>', range=[-0.5, 8], zeroline=True, scaleanchor='x'),
    showlegend=True, margin=dict(t=20, b=60, l=55, r=20))
fig.show()
```

## Summary {#sec-summary}

::: {.callout-note appearance="simple"}
## Key Takeaways

1. **Every LP has a dual LP** obtained by introducing multipliers for each constraint. The dual provides bounds on the primal's optimal value.

2. **Weak duality** always holds: every primal feasible value $\leq$ every dual feasible value. This gives certificates of optimality: if the gap is zero, both solutions are optimal.

3. **Strong duality** holds at optimality: the primal and dual optimal values are equal ($c^\top x^* = b^\top y^*$). The proof constructs the dual optimal solution from the simplex tableau.

4. **Shadow prices** ($y_i^*$) measure the sensitivity of the optimal value to changes in the right-hand side: $y_i^* = \partial f^* / \partial b_i$.

5. **Complementary slackness** characterizes optimality pointwise: at an optimal pair, if a variable is positive, the corresponding constraint is tight, and vice versa.

6. The **four possibilities** for a primal-dual pair are: both infeasible, primal infeasible & dual unbounded, primal unbounded & dual infeasible, or both finite optimal (with zero duality gap).
:::

::: {.callout-tip}
## Looking Ahead

Duality theory is not just a theoretical curiosity -- it is the engine behind some of the most important LP algorithms. In the [next chapter](12-dual-simplex.qmd), we will see how the **dual simplex method** maintains dual feasibility and works toward primal feasibility, essentially solving the dual LP while recovering a primal solution. We will also explore how duality underpins important [LP applications](13-lp-applications.qmd) including two-person zero-sum games and robust optimization.
:::
